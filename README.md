Standart python calculator with all the basic math functions and the implementation of a GUI.
How to use it: 
If you want the gui:
  run the command pip install customtkinter on your terminal to download the UI library
If you just want the calculator:
  run the code on calculator(simplified), it still has all the functions but runs on the terminal.
